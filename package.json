{
  "name": "linux-command",
  "version": "1.0.0",
  "private": true,
  "description": "A fast static Linux command handbook powered by markdown files.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm run build && node scripts/check.mjs",
    "dev": "node scripts/dev-server.mjs",
    "clean": "node scripts/clean.mjs"
  },
  "keywords": [
    "linux",
    "commands",
    "handbook",
    "static-site"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
